name: C/C++ CI

on: [push]

jobs:
  build:
  
    runs-on: ubuntu-16.04
    
    steps:
    - name: checkout
      uses: actions/checkout@v1
    - name: build
    - uses: ./.github/actions/buid-action
